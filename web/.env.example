# Optional: Override API URL (default: relative URLs, works from any host)
# Only needed if your backend runs on a different origin than the frontend.
# In dev mode, Vite's proxy handles forwarding automatically.
# VITE_API_URL=http://localhost:8080
