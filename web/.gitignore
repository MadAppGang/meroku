# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static

# Foreign lockfiles — pnpm-lock.yaml is the single source of truth.
# CI enforces it with `pnpm install --frozen-lockfile` (.github/workflows/release.yml).
bun.lock
bun.lockb
package-lock.json
yarn.lock
