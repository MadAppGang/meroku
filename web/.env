# API URL is NOT set here - the built SPA uses relative URLs so it works
# from any host (localhost, Tailscale, etc.)
# In dev mode (npm run dev), Vite's proxy config handles forwarding to the Go backend.
# Only set VITE_API_URL if you need to point at a different backend:
# VITE_API_URL=http://localhost:8080
